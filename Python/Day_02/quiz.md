Answer these in your own words.

What does input() do?
Captures the value from the user by providing a prompt.

What data type does input() return?
Always returns a string

Why do we use int()?
To convert the value into an integar value

What is the difference between / and //?
/ denotes division which returns the value float and // denotes floor division which returns only the whole number.

What does % return?
returns the reminder value

What does ** do?
this returns power of value

What is a comment?
comments are notes for the developers to understand the code in a better and quicker way

Does Python execute comments?
no, pythod will exclude the comments from execution.